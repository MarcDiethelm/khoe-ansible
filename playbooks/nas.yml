---
# usage:
# ansible-playbook playbooks/nas.yml -e task=create -e username=example1 -e password=1234 [-e share_path=/home/example1/nas]
# ansible-playbook playbooks/nas.yml # (re-)start all shares

- hosts: khoe
  gather_facts: no

  tasks:
    # - include_role:
    #     name: pmount
    - include_role:
        name: docker-smb
