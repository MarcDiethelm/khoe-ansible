defaults:
  share:
      path: '{{ share_path }}'
      users: '{{ username }}'
      browsable: 'yes'
      readonly: 'no'
      writelist: ''
      guest: 'no'
      admins: 'none'
      comment: "{{ username }}''s private NAS"

  user:
      smbpsw: '{{ smbpassword | default(password) }}'
      userid: '{{ user.uid }}'
      groupid: '{{ user.gid }}'
