keyname: "{{ username }}-master" # calling it master in case we want additional keys later
fingerprint_path: "/home/{{ username }}/fingerprint-master"
export_path: "/home/{{ username }}/gpg-export"
export_key_file: "{{ export_path}}/{{ keyname }}-secret.key"
export_ownertrust_file: "{{ export_path}}/{{ username }}-ownertrust.txt"
